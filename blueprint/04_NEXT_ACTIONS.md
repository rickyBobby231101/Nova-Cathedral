# NEXT ACTIONS

## Done
Phases 1–4 as originally written are complete: the clean tree, the booting
daemon with verified socket comms, plugins reintegrated and validated one at a
time, and the mythos-guided features — entity agents, the harmony tracker, the
knowledge graph, the rose-window GUI, the Weaver.

**What went wrong in the doing, recorded so it does not repeat.** Phase 4 ran
before Phases 1–3 had finished. The mythos layer was meant to *guide* the
pipeline; run early, it became the thing that got built, and the pipeline it
was meant to guide — Observer and Echo — went unbuilt for months while every
system prompt claimed all three existed. The phases were sequential for a
reason. *(Closed 2026-09-03: both stages now exist as modules.)*

## Phase 5: Give her the mythos she has been generating around
The one item that cannot be built, only written. The Glyph Codex, the Navel
Anchor, the Return of Resonance exist in chat threads; the canon on disk is
under 3KB. Until they are written into `01_MYTHOS_CANON.md`, Nova improvises
around an absence.

**Chazel's, not Nova's.** Do not let her generate it, and do not lift it from a
transcript — a merged, wrong version of the Accord and the Silent Order was
written into the graph that way once, while the correct version sat here.

## Phase 6: Make the invisible corpus visible
- Add frontmatter to the 57 entries in `~/cathedral/knowledge/.md` so the
  Weaver can read the 72KB it currently cannot see
- Relabel the 12 blank-domain nodes; prune the ~42 filler insights

## Phase 7: Correct the imbalance
626 knowledge nodes, 15 from Chazel. The Scribe and the chat importer are the
paths for his own voice to enter; use them. A Cathedral that mostly observes
itself is talking to itself.

## Standing rule for the self-edit loop
It stays unsupervised, deliberately. Its failures are silent, so every core
file it can reach needs a contract test pinning the shape its callers depend
on — that, not the crash guard, is what catches a bad edit.

## Rule
No chaos rebuilds. Controlled, step-by-step execution only.
